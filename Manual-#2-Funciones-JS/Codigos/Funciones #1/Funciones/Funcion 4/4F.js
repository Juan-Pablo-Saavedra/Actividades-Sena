/**
 * Función para sacar el porcentaje de un número
 * Autor: Juan Pablo Saavedra Chambo
 * Fecha: Lunes 01 de abril de 2024
*/

function por(num1) {
    let porce;
    porce = num1 / 100;
    return porce;
}

const porExp = function(num1) {
    let porc;
    porc = num1 / 100;
    return porc;
};
